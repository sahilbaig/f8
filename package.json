{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "description": "F8Tech-Backend in JS ",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "NODE_ENV=dev nodemon -r dotenv/config app.js",
    "start:test": "NODE_ENV=test node -r dotenv/config app.js",
    "start:prod": "NODE_ENV=production node -r dotenv/config app.js"
  },
  "author": "sahil",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
